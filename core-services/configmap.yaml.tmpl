---
apiVersion: v1
kind: ConfigMap
metadata:
  name: eks-params
  namespace: core-services
data:
  name: "{{ .ClusterName }}"
  region: "{{ .Region }}"
  eks:
    cluster:
      name: "{{ .ClusterName }}"
      region: "{{ .Region }}"
